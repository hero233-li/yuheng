import axios from'axios';export const cardStatusClient=axios.create({baseURL:'/api',timeout:15000});cardStatusClient.interceptors.request.use(config=>{const token=localStorage.getItem('alioth_token');if(token)config.headers.Authorization=`Bearer ${token}`;return config});cardStatusClient.interceptors.response.use(response=>response,error=>{if(error.response?.status===401){localStorage.removeItem('alioth_token');window.dispatchEvent(new Event('alioth:logout'))}return Promise.reject(new Error(error.response?.data?.message||error.message||'请求失败'))});

import{Button,Card,Col,Form,Input,Row,Select,type FormInstance}from'antd';import{Search}from'lucide-react';import{cardStatusConfig}from'../config/cardStatusConfig';import type{CardSearchValues}from'../types';
export default function CardSearchForm({form,busy,onSubmit}:{form:FormInstance<CardSearchValues>;busy:boolean;onSubmit:(v:CardSearchValues)=>void}){return <Card title="卡状态查询"><Form form={form} layout="vertical" disabled={busy} initialValues={{environment:cardStatusConfig.environments[0]}} onFinish={onSubmit}><Row gutter={16}><Col span={12}><Form.Item name="environment" label="环境" rules={[{required:true}]}><Select options={cardStatusConfig.environments.map(value=>({value,label:value}))}/></Form.Item></Col><Col span={12}><Form.Item name="customerNo" label="客户号" rules={[{required:true}]}><Input/></Form.Item></Col></Row><div className="card-status-actions"><Button type="primary" htmlType="submit" icon={<Search size={16}/>}>查询</Button></div></Form></Card>}

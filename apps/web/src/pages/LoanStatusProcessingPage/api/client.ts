import axios from'axios';export const loanStatusClient=axios.create({baseURL:'/api',timeout:15000});loanStatusClient.interceptors.request.use(config=>{const token=localStorage.getItem('alioth_token');if(token)config.headers.Authorization=`Bearer ${token}`;return config});loanStatusClient.interceptors.response.use(r=>r,e=>Promise.reject(new Error(e.response?.data?.message||e.message||'请求失败')));

import{useRef,useState}from'react';import{message,Modal}from'antd';import{LoanJobError,pollLoanJob,retryLoanJob,submitLoanAction,submitLoanSearch}from'../api/loanStatus';import{loanActionLabel}from'../config/loanStatusConfig';import type{LoanAction,LoanActionValues,LoanActivity,LoanCardRecord,LoanInterruptedFlow,LoanJob,LoanResultRecord,LoanSearchValues,LoanVoucher}from'../types';
const numberValue=(value:unknown)=>{const number=Number(value);return Number.isFinite(number)?number:0};
const loanStatus=(value:unknown):LoanResultRecord['loanStatus']=>value==='已生效'||value==='已结束'||value==='未签署'?value:'未签署';
const normalizeVoucher=(voucher:LoanVoucher):LoanVoucher=>{const nextRepaymentDate=voucher.nextRepaymentDate||voucher.dueDate||'-';const status=voucher.status==='正常'?'使用中':voucher.status;const outstandingAmount=numberValue(voucher.outstandingAmount);const repaymentPlan=Array.isArray(voucher.repaymentPlan)&&voucher.repaymentPlan.length?voucher.repaymentPlan.map(item=>({...item,status:item.status==='使用中'||item.status==='已结清'||item.status==='逾期'?item.status:'使用中',principal:numberValue(item.principal),interest:numberValue(item.interest),totalAmount:numberValue(item.totalAmount)})):[{installmentNo:1,repaymentDate:nextRepaymentDate,principal:outstandingAmount,interest:0,totalAmount:outstandingAmount,status:status==='正常'?'使用中':status}];return{...voucher,status,nextRepaymentDate,drawAmount:numberValue(voucher.drawAmount),outstandingAmount,overdueAmount:numberValue(voucher.overdueAmount),repaidPrincipal:numberValue(voucher.repaidPrincipal),repaidInterest:numberValue(voucher.repaidInterest),outstandingPrincipal:Number.isFinite(Number(voucher.outstandingPrincipal))?Number(voucher.outstandingPrincipal):outstandingAmount,outstandingInterest:numberValue(voucher.outstandingInterest),repaymentPlan}};
const toResultRows=(cards:LoanCardRecord[]):LoanResultRecord[]=>cards.reduce<LoanResultRecord[]>((rows,card)=>{const common={environment:card.environment,customerNo:card.customerNo,customerName:card.customerName,certificateNo:card.certificateNo,phone:card.phone,cardNo:card.cardNo,balance:numberValue(card.balance),cardStatus:card.status,freezeStatus:card.freezeStatus,quotaNo:card.quotaNo,signDate:'-',organizationNo:'-',relationshipManager:'-',accountingDate:'-',graceDays:0,coreRate:0,generalAccountingDate:'-',parameterAccountingDate:'-'};if(!card.loans.length){rows.push({...common,rowKey:`${card.cardNo}:none`,contractNo:'',loanStatus:'无关联合同信息',freezeStatus:'-',overdueStatus:'-',debt:0,overdueDebt:0,creditLimit:0,usedCredit:0,availableCredit:0,vouchers:[]});return rows}rows.push(...card.loans.map<LoanResultRecord>(loan=>{const status=loanStatus(loan.status);const creditLimit=numberValue(loan.creditLimit);const usedCredit=numberValue(loan.usedCredit);const vouchers=Array.isArray(loan.vouchers)?loan.vouchers.map(normalizeVoucher):[];return{...common,rowKey:loan.contractNo,contractNo:loan.contractNo,quotaNo:loan.quotaNo||card.quotaNo||'-',signDate:status==='未签署'?'-':loan.signDate||'-',organizationNo:loan.organizationNo||'-',relationshipManager:loan.relationshipManager||'-',accountingDate:loan.accountingDate||'-',graceDays:numberValue(loan.graceDays),coreRate:numberValue(loan.coreRate),generalAccountingDate:loan.generalAccountingDate||'-',parameterAccountingDate:loan.parameterAccountingDate||'-',loanStatus:status,freezeStatus:status==='未签署'?'-':loan.freezeStatus==='是'?'是':'否',overdueStatus:status==='未签署'?'-':loan.overdueStatus==='是'?'是':'否',debt:numberValue(loan.debt),overdueDebt:numberValue(loan.overdueDebt),creditLimit,usedCredit,availableCredit:Number.isFinite(Number(loan.availableCredit))?Number(loan.availableCredit):Math.max(0,creditLimit-usedCredit),vouchers}}));return rows},[]);
export function useLoanStatusProcessing(){const[records,setRecords]=useState<LoanResultRecord[]>([]);const[activity,setActivity]=useState<LoanActivity|null>(null);const[interrupted,setInterrupted]=useState<LoanInterruptedFlow|null>(null);const lastSearch=useRef<LoanSearchValues|null>(null);const poll=(id:number,label:string)=>pollLoanJob(id,j=>setActivity({jobId:id,label,status:j.status,progress:j.progress,currentStep:j.currentStep}));const run=async(label:string,submitter:()=>Promise<{id:number;status:LoanJob['status'];progress:number}>)=>{setActivity({label,status:'submitting',progress:0});const s=await submitter();return poll(s.id,label)};const update=(card:LoanCardRecord)=>setRecords(current=>{const index=current.findIndex(item=>item.cardNo===card.cardNo);const next=current.filter(item=>item.cardNo!==card.cardNo);next.splice(index<0?next.length:index,0,...toResultRows([card]));return next});const refresh=async()=>{if(!lastSearch.current)return;try{const j=await run('正在刷新贷款状态',()=>submitLoanSearch(lastSearch.current as LoanSearchValues));setRecords(toResultRows((j.result.cards as LoanCardRecord[])??[]))}catch(e){message.warning(e instanceof Error?`操作已完成，但刷新失败：${e.message}`:'操作已完成，但刷新失败')}};
 const search=async(v:LoanSearchValues)=>{lastSearch.current=v;setRecords([]);try{const j=await run('正在查询贷款合同',()=>submitLoanSearch(v));setRecords(toResultRows((j.result.cards as LoanCardRecord[])??[]))}catch(e){message.error(e instanceof Error?e.message:'查询失败')}finally{setActivity(null)}};
 const execute=async(card:LoanResultRecord,action:LoanAction,v:LoanActionValues)=>{try{const j=await run(`正在${loanActionLabel(action)}`,()=>submitLoanAction(card.cardNo,action,v));if(action==='freeze'||action==='unfreeze'){update(j.result.card as LoanCardRecord);Modal.success({title:String(j.result.message??`${loanActionLabel(action)}完成`)})}else{const result=j.result.actionResult as{card:LoanCardRecord;message:string};update(result.card);if(action==='contract-sign')Modal.success({title:result.message});else message.success(result.message)}setInterrupted(null);await refresh();return true}catch(e){if(e instanceof LoanJobError&&(action==='freeze'||action==='unfreeze')){setInterrupted({jobId:e.job.id,cardNo:card.cardNo,action,currentStep:e.job.currentStep});message.warning('流程已中断，可继续处理')}else message.error(e instanceof Error?e.message:'操作失败');return false}finally{setActivity(null)}};
 const resume=async(card:LoanResultRecord)=>{if(!interrupted)return false;try{await retryLoanJob(interrupted.jobId);setActivity({jobId:interrupted.jobId,label:`继续${loanActionLabel(interrupted.action)}`,status:'retrying',progress:0});const j=await poll(interrupted.jobId,`继续${loanActionLabel(interrupted.action)}`);update(j.result.card as LoanCardRecord);Modal.success({title:String(j.result.message??'处理完成')});setInterrupted(null);await refresh();return true}catch(e){message.error(e instanceof Error?e.message:'继续处理失败');return false}finally{setActivity(null)}};
 const queryRepaymentPlan=async(card:LoanResultRecord,voucherNo:string):Promise<LoanVoucher|null>=>{try{if(!lastSearch.current)return card.vouchers.find(item=>item.voucherNo===voucherNo)??null;const j=await run('正在查询还款计划',()=>submitLoanSearch(lastSearch.current as LoanSearchValues));const rows=toResultRows((j.result.cards as LoanCardRecord[])??[]);setRecords(rows);return rows.find(item=>item.contractNo===card.contractNo)?.vouchers.find(item=>item.voucherNo===voucherNo)??null}catch(e){message.error(e instanceof Error?e.message:'还款计划查询失败');return null}finally{setActivity(null)}};
 return{records,activity,busy:Boolean(activity),interrupted,search,execute,resume,queryRepaymentPlan};}
